# 网络同步

```lua
-- ============================================================
-- 网络同步 — 服务端与客户端之间的数据通信
-- DST 是联网游戏，服务端计算 + 客户端渲染，需要用网络变量/RPC 同步状态
-- ============================================================

-- ──────────────────────────────
-- 概述：服务端 vs 客户端
-- ──────────────────────────────

-- 服务端（TheWorld.ismastersim == true）:
--   负责逻辑计算、数据存储、状态管理
-- 客户端（TheWorld.ismastersim ~= true）:
--   负责 UI 渲染、音效、特效、玩家输入

-- 主机玩家（host）同时运行服务端+客户端。
-- 专用服务器只有服务端逻辑。
-- 普通客户端只运行客户端逻辑。

-- modmain.lua 的顶层代码双端执行。
-- 写在 Prefab fn() 内的代码也在双端分别执行（各执行各的 inst）。

-- ──────────────────────────────
-- 判别当前执行环境
-- ──────────────────────────────

-- if TheWorld.ismastersim then
--     -- 服务端逻辑
--     print("这段代码只在服务端运行")
-- else
--     -- 客户端逻辑
--     print("这段代码只在客户端运行")
-- end

-- ──────────────────────────────
-- 网络变量（net_var）— 自动双向同步
-- ──────────────────────────────

-- 在网络组件（inst.Network 或 inst.components.network）上注册 net_var，
-- 服务端修改后自动推送到所有客户端。

-- 在 Prefab 的 fn() 中注册（双端都会执行）:
-- inst.entity:AddNetwork()  -- 必须首先添加网络实体
-- inst.Network:AddNetworkVar("my_value", "net_float")  -- 浮点数
-- inst.Network:AddNetworkVar("my_flag", "net_bool")    -- 布尔值

-- net_var 类型：
--   "net_float"  — 小数/浮点数
--   "net_bool"   — 布尔
--   "net_int"    — 整数
--   "net_string" — 字符串
--   "net_entity" — 实体引用（其他实体）
--   "net_byte"   — 0-255 的字节
--   "net_short"  — 短整数

-- 服务端修改（写）:
-- if TheWorld.ismastersim then
--     inst.Network:SetMyValue(42)
-- end

-- 客户端读取（读）:
-- local val = inst.Network:GetMyValue()

-- 监听同步完成事件:
-- inst:ListenForEvent("my_value_dirty", function(inst)
--     -- 当服务端推送新的 my_value 时触发
--     print("my_value 已更新为: " .. tostring(inst.Network:GetMyValue()))
-- end)

-- ──────────────────────────────
-- 自定义 RPC（远程过程调用）
-- ──────────────────────────────

-- 注册 RPC handler（双端都注册以确保收到对方的调用）

-- 服务端 → 所有客户端（广播）
-- 在 modmain.lua 中注册:
-- AddModRPCHandler("my_mod_namespace", "AnnounceSomething", function(player, data)
--     -- 此函数在客户端执行
--     if player and player.components.talker then
--         player.components.talker:Say(tostring(data))
--     end
-- end)

-- 服务端发起调用:
-- if TheWorld.ismastersim then
--     SendModRPCToClient(
--         GetClientTable(),        -- 发给谁？所有客户端
--         "my_mod_namespace",             -- 命名空间
--         "AnnounceSomething",            -- 事件名
--         "你好，所有玩家！"               -- 自定义数据（string/number/bool）
--     )
-- end

-- 客户端 → 服务端（单向请求）
-- 注册 handler:
-- AddModRPCHandler("my_mod_namespace", "PlayerClickedButton", function(player, data)
--     -- 此函数在服务端执行
--     if player then
--         print("玩家 " .. player:GetDisplayName() .. " 按下了按钮: " .. tostring(data))
--     end
-- end)

-- 客户端发起调用:
-- SendModRPCToServer(
--     "my_mod_namespace",
--     "PlayerClickedButton",
--     "button_1"
-- )

-- ──────────────────────────────
-- net_event — 单向推送（轻量）
-- ──────────────────────────────

-- 比 RPC 更轻量，适合频繁触发的事件
-- 在 Prefab fn() 中注册:
-- inst.entity:AddNetwork()
-- inst:ListenForEvent("my_custom_event", function(inst)
--     -- 收到事件后的处理
--     inst:PushEvent("on_my_custom_event_complete")
-- end)

-- 服务端推送:
-- if TheWorld.ismastersim then
--     inst:PushEvent("my_custom_event")
-- end

-- ──────────────────────────────
-- 常见同步场景
-- ──────────────────────────────

-- 场景 1：自定义血量条（客户端 UI 需要知道血量）
-- 在 Prefab fn() 中:
--   inst.Network:AddNetworkVar("max_energy", "net_float")
-- 服务端设置:
--   if TheWorld.ismastersim then
--       inst.Network:SetMaxEnergy(100)
--   end
-- 客户端读取用于 UI:
--   local energy = inst.Network:GetMaxEnergy()

-- 场景 2：玩家右键自定义物品触发特效
-- 客户端检测右键 → SendModRPCToServer → 服务端处理 → PushEvent 通知所有客户端
--   → 其它客户端播放特效

-- 场景 3：同步世界状态（如 mod 天气）
-- 使用 net_var 在 world 实体上注册 WorldState 变量。
-- 服务端每帧或定时更新，客户端自动同步。

-- ──────────────────────────────
-- 注意事项
-- ──────────────────────────────

-- 1. 所有 net_var 必须在 inst.entity:AddNetwork() 之后、inst 生成完成前注册。
-- 2. 不要在客户端直接修改 net_var——只读！修改由服务端负责。
-- 3. RPC 命名空间（"my_mod_namespace"）要唯一，防止与其他 mod 冲突。
-- 4. 网络传输数据有大小限制，不要传大字符串或表，必要时拆分传输。
-- 5. 单机玩时（无网络），SendModRPCToClient 仍是安全的，框架会内部处理。
```
